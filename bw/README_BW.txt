README.txt

Blue Waters has XE nodes with 32 integer cores per node. and XK Nodes with 16 integer cores per node.

The default, unlableled measurements are for the XE Nodes.
The XK Nodes are labelled.